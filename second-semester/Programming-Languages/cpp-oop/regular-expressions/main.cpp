#include <iostream>
#include <string>
using namespace std;

int main(){
    //leer la expresion
    cout << "Escriba la expresión: " << endl;
    string expression = "";
    getline(cin, expression);
    // cin << expression;

    // Verificador* veri = new Verificador(); //Comprueba si la expresion es valida
}